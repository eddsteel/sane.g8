name := "$name$"
description := "$pdescription$"
scalaVersion := "2.12.2"
organization := "$organization$"
version := "slice1"

libraryDependencies += "org.typelevel" %% "cats" % "0.9.0"

Lint.settings
Flags.settings
