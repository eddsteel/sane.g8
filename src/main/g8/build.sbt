name := "$name$"
description := "$desc$"
scalaVersion := "2.12.3"
organization := "$organization$"
version := "slice1"

libraryDependencies += "org.typelevel" %% "cats" % "1.0.0-MF"

Lint.settings
Flags.settings
